:component BasicFML (
    :state count = 0
    (
        div class="flex items-center justify-center w-full" (
            h1 (
                ["Counter App"]
            )
            button @click . set(count, count+1) (
                ["Increment"]
            )
            p (
                [count]
            )
        )
    )
)
